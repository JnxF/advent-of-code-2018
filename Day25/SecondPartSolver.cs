using Helpers;

namespace Day25
{
    class SecondPartSolver : ISolver<string>
    {
        private readonly string _input;

        public SecondPartSolver(string input)
        {
            _input = input;
        }

        public string Solve()
        {
            return "Not enough stars for the second part :)";
        }
    }
}
